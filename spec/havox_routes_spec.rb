require "spec_helper"

describe HavoxRoutes do
  it "has a version number" do
    expect(HavoxRoutes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
